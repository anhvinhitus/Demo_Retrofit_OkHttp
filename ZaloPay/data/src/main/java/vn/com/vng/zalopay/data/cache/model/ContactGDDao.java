package vn.com.vng.zalopay.data.cache.model;

import java.util.List;
import java.util.ArrayList;
import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.SqlUtils;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "CONTACT_GD".
*/
public class ContactGDDao extends AbstractDao<ContactGD, Long> {

    public static final String TABLENAME = "CONTACT_GD";

    /**
     * Properties of entity ContactGD.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property Fulltextsearch = new Property(0, String.class, "fulltextsearch", false, "FULLTEXTSEARCH");
        public final static Property DisplayName = new Property(1, String.class, "displayName", false, "DISPLAY_NAME");
        public final static Property PhoneNumber = new Property(2, long.class, "phoneNumber", true, "PHONE_NUMBER");
    }

    private DaoSession daoSession;


    public ContactGDDao(DaoConfig config) {
        super(config);
    }
    
    public ContactGDDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
        this.daoSession = daoSession;
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"CONTACT_GD\" (" + //
                "\"FULLTEXTSEARCH\" TEXT," + // 0: fulltextsearch
                "\"DISPLAY_NAME\" TEXT," + // 1: displayName
                "\"PHONE_NUMBER\" INTEGER PRIMARY KEY NOT NULL );"); // 2: phoneNumber
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"CONTACT_GD\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, ContactGD entity) {
        stmt.clearBindings();
 
        String fulltextsearch = entity.fulltextsearch;
        if (fulltextsearch != null) {
            stmt.bindString(1, fulltextsearch);
        }
 
        String displayName = entity.displayName;
        if (displayName != null) {
            stmt.bindString(2, displayName);
        }
        stmt.bindLong(3, entity.phoneNumber);
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, ContactGD entity) {
        stmt.clearBindings();
 
        String fulltextsearch = entity.fulltextsearch;
        if (fulltextsearch != null) {
            stmt.bindString(1, fulltextsearch);
        }
 
        String displayName = entity.displayName;
        if (displayName != null) {
            stmt.bindString(2, displayName);
        }
        stmt.bindLong(3, entity.phoneNumber);
    }

    @Override
    protected final void attachEntity(ContactGD entity) {
        super.attachEntity(entity);
        entity.__setDaoSession(daoSession);
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.getLong(offset + 2);
    }    

    @Override
    public ContactGD readEntity(Cursor cursor, int offset) {
        ContactGD entity = new ContactGD();
        readEntity(cursor, entity, offset);
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, ContactGD entity, int offset) {
        entity.fulltextsearch = cursor.isNull(offset + 0) ? null : cursor.getString(offset + 0);
        entity.displayName = cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1);
        entity.phoneNumber = cursor.getLong(offset + 2);
     }
    
    @Override
    protected final Long updateKeyAfterInsert(ContactGD entity, long rowId) {
        entity.phoneNumber = rowId;
        return rowId;
    }
    
    @Override
    public Long getKey(ContactGD entity) {
        if(entity != null) {
            return entity.phoneNumber;
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(ContactGD entity) {
        throw new UnsupportedOperationException("Unsupported for entities with a non-null key");
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
    private String selectDeep;

    protected String getSelectDeep() {
        if (selectDeep == null) {
            StringBuilder builder = new StringBuilder("SELECT ");
            SqlUtils.appendColumns(builder, "T", getAllColumns());
            builder.append(',');
            SqlUtils.appendColumns(builder, "T0", daoSession.getZaloPayProfileGDDao().getAllColumns());
            builder.append(" FROM CONTACT_GD T");
            builder.append(" LEFT JOIN ZALO_PAY_PROFILE_GD T0 ON T.\"PHONE_NUMBER\"=T0.\"ZALO_ID\"");
            builder.append(' ');
            selectDeep = builder.toString();
        }
        return selectDeep;
    }
    
    protected ContactGD loadCurrentDeep(Cursor cursor, boolean lock) {
        ContactGD entity = loadCurrent(cursor, 0, lock);
        int offset = getAllColumns().length;

        ZaloPayProfileGD zaloPayInfo = loadCurrentOther(daoSession.getZaloPayProfileGDDao(), cursor, offset);
         if(zaloPayInfo != null) {
            entity.setZaloPayInfo(zaloPayInfo);
        }

        return entity;    
    }

    public ContactGD loadDeep(Long key) {
        assertSinglePk();
        if (key == null) {
            return null;
        }

        StringBuilder builder = new StringBuilder(getSelectDeep());
        builder.append("WHERE ");
        SqlUtils.appendColumnsEqValue(builder, "T", getPkColumns());
        String sql = builder.toString();
        
        String[] keyArray = new String[] { key.toString() };
        Cursor cursor = db.rawQuery(sql, keyArray);
        
        try {
            boolean available = cursor.moveToFirst();
            if (!available) {
                return null;
            } else if (!cursor.isLast()) {
                throw new IllegalStateException("Expected unique result, but count was " + cursor.getCount());
            }
            return loadCurrentDeep(cursor, true);
        } finally {
            cursor.close();
        }
    }
    
    /** Reads all available rows from the given cursor and returns a list of new ImageTO objects. */
    public List<ContactGD> loadAllDeepFromCursor(Cursor cursor) {
        int count = cursor.getCount();
        List<ContactGD> list = new ArrayList<ContactGD>(count);
        
        if (cursor.moveToFirst()) {
            if (identityScope != null) {
                identityScope.lock();
                identityScope.reserveRoom(count);
            }
            try {
                do {
                    list.add(loadCurrentDeep(cursor, false));
                } while (cursor.moveToNext());
            } finally {
                if (identityScope != null) {
                    identityScope.unlock();
                }
            }
        }
        return list;
    }
    
    protected List<ContactGD> loadDeepAllAndCloseCursor(Cursor cursor) {
        try {
            return loadAllDeepFromCursor(cursor);
        } finally {
            cursor.close();
        }
    }
    

    /** A raw-style query where you can pass any WHERE clause and arguments. */
    public List<ContactGD> queryDeep(String where, String... selectionArg) {
        Cursor cursor = db.rawQuery(getSelectDeep() + where, selectionArg);
        return loadDeepAllAndCloseCursor(cursor);
    }
 
}
