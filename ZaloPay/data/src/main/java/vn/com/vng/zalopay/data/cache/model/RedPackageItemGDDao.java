package vn.com.vng.zalopay.data.cache.model;

import android.database.Cursor;
import android.database.sqlite.SQLiteDatabase;
import android.database.sqlite.SQLiteStatement;

import de.greenrobot.dao.AbstractDao;
import de.greenrobot.dao.Property;
import de.greenrobot.dao.internal.DaoConfig;

import vn.com.vng.zalopay.data.cache.model.RedPackageItemGD;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "RED_PACKAGE_ITEM_GD".
*/
public class RedPackageItemGDDao extends AbstractDao<RedPackageItemGD, Long> {

    public static final String TABLENAME = "RED_PACKAGE_ITEM_GD";

    /**
     * Properties of entity RedPackageItemGD.<br/>
     * Can be used for QueryBuilder and for referencing column names.
    */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property ZpTransID = new Property(1, String.class, "zpTransID", false, "ZP_TRANS_ID");
        public final static Property BundleID = new Property(2, Long.class, "bundleID", false, "BUNDLE_ID");
        public final static Property State = new Property(3, Integer.class, "state", false, "STATE");
    };


    public RedPackageItemGDDao(DaoConfig config) {
        super(config);
    }
    
    public RedPackageItemGDDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(SQLiteDatabase db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"RED_PACKAGE_ITEM_GD\" (" + //
                "\"_id\" INTEGER PRIMARY KEY ," + // 0: id
                "\"ZP_TRANS_ID\" TEXT," + // 1: zpTransID
                "\"BUNDLE_ID\" INTEGER," + // 2: bundleID
                "\"STATE\" INTEGER);"); // 3: state
    }

    /** Drops the underlying database table. */
    public static void dropTable(SQLiteDatabase db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"RED_PACKAGE_ITEM_GD\"";
        db.execSQL(sql);
    }

    /** @inheritdoc */
    @Override
    protected void bindValues(SQLiteStatement stmt, RedPackageItemGD entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String zpTransID = entity.getZpTransID();
        if (zpTransID != null) {
            stmt.bindString(2, zpTransID);
        }
 
        Long bundleID = entity.getBundleID();
        if (bundleID != null) {
            stmt.bindLong(3, bundleID);
        }
 
        Integer state = entity.getState();
        if (state != null) {
            stmt.bindLong(4, state);
        }
    }

    /** @inheritdoc */
    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    /** @inheritdoc */
    @Override
    public RedPackageItemGD readEntity(Cursor cursor, int offset) {
        RedPackageItemGD entity = new RedPackageItemGD( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1), // zpTransID
            cursor.isNull(offset + 2) ? null : cursor.getLong(offset + 2), // bundleID
            cursor.isNull(offset + 3) ? null : cursor.getInt(offset + 3) // state
        );
        return entity;
    }
     
    /** @inheritdoc */
    @Override
    public void readEntity(Cursor cursor, RedPackageItemGD entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setZpTransID(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
        entity.setBundleID(cursor.isNull(offset + 2) ? null : cursor.getLong(offset + 2));
        entity.setState(cursor.isNull(offset + 3) ? null : cursor.getInt(offset + 3));
     }
    
    /** @inheritdoc */
    @Override
    protected Long updateKeyAfterInsert(RedPackageItemGD entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    /** @inheritdoc */
    @Override
    public Long getKey(RedPackageItemGD entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    /** @inheritdoc */
    @Override    
    protected boolean isEntityUpdateable() {
        return true;
    }
    
}
