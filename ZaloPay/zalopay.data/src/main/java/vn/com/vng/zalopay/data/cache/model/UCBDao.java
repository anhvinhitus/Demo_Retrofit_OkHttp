package vn.com.vng.zalopay.data.cache.model;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "UCB".
*/
public class UCBDao extends AbstractDao<UCB, Long> {

    public static final String TABLENAME = "UCB";

    /**
     * Properties of entity UCB.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property PhoneNumber = new Property(1, String.class, "phoneNumber", false, "PHONE_NUMBER");
        public final static Property DisplayName = new Property(2, String.class, "displayName", false, "DISPLAY_NAME");
        public final static Property NormalizeDisplayName = new Property(3, String.class, "normalizeDisplayName", false, "NORMALIZE_DISPLAY_NAME");
        public final static Property PhotoUri = new Property(4, String.class, "photoUri", false, "PHOTO_URI");
        public final static Property FirstName = new Property(5, String.class, "firstName", false, "FIRST_NAME");
        public final static Property LastName = new Property(6, String.class, "lastName", false, "LAST_NAME");
    }


    public UCBDao(DaoConfig config) {
        super(config);
    }
    
    public UCBDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"UCB\" (" + //
                "\"_id\" INTEGER PRIMARY KEY AUTOINCREMENT ," + // 0: id
                "\"PHONE_NUMBER\" TEXT NOT NULL UNIQUE ," + // 1: phoneNumber
                "\"DISPLAY_NAME\" TEXT," + // 2: displayName
                "\"NORMALIZE_DISPLAY_NAME\" TEXT," + // 3: normalizeDisplayName
                "\"PHOTO_URI\" TEXT," + // 4: photoUri
                "\"FIRST_NAME\" TEXT," + // 5: firstName
                "\"LAST_NAME\" TEXT);"); // 6: lastName
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"UCB\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, UCB entity) {
        stmt.clearBindings();
 
        Long id = entity.id;
        if (id != null) {
            stmt.bindLong(1, id);
        }
        stmt.bindString(2, entity.phoneNumber);
 
        String displayName = entity.displayName;
        if (displayName != null) {
            stmt.bindString(3, displayName);
        }
 
        String normalizeDisplayName = entity.normalizeDisplayName;
        if (normalizeDisplayName != null) {
            stmt.bindString(4, normalizeDisplayName);
        }
 
        String photoUri = entity.photoUri;
        if (photoUri != null) {
            stmt.bindString(5, photoUri);
        }

        String firstName = entity.firstName;
        if (firstName != null) {
            stmt.bindString(6, firstName);
        }

        String lastName = entity.lastName;
        if (lastName != null) {
            stmt.bindString(7, lastName);
        }
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, UCB entity) {
        stmt.clearBindings();
 
        Long id = entity.id;
        if (id != null) {
            stmt.bindLong(1, id);
        }
        stmt.bindString(2, entity.phoneNumber);
 
        String displayName = entity.displayName;
        if (displayName != null) {
            stmt.bindString(3, displayName);
        }
 
        String normalizeDisplayName = entity.normalizeDisplayName;
        if (normalizeDisplayName != null) {
            stmt.bindString(4, normalizeDisplayName);
        }
 
        String photoUri = entity.photoUri;
        if (photoUri != null) {
            stmt.bindString(5, photoUri);
        }

        String firstName = entity.firstName;
        if (firstName != null) {
            stmt.bindString(6, firstName);
        }

        String lastName = entity.lastName;
        if (lastName != null) {
            stmt.bindString(7, lastName);
        }
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public UCB readEntity(Cursor cursor, int offset) {
        UCB entity = new UCB();
        readEntity(cursor, entity, offset);
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, UCB entity, int offset) {
        entity.id = cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
        entity.phoneNumber = cursor.getString(offset + 1);
        entity.displayName = cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2);
        entity.normalizeDisplayName = cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3);
        entity.photoUri = cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4);
        entity.firstName = cursor.isNull(offset + 5) ? null : cursor.getString(offset + 5);
        entity.lastName = cursor.isNull(offset + 6) ? null : cursor.getString(offset + 6);
     }
    
    @Override
    protected final Long updateKeyAfterInsert(UCB entity, long rowId) {
        entity.id = rowId;
        return rowId;
    }
    
    @Override
    public Long getKey(UCB entity) {
        if(entity != null) {
            return entity.id;
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(UCB entity) {
        return entity.id != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
