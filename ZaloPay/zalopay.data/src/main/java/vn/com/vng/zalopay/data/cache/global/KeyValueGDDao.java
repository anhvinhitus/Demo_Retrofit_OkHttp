package vn.com.vng.zalopay.data.cache.global;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "KEY_VALUE_GD".
*/
public class KeyValueGDDao extends AbstractDao<KeyValueGD, String> {

    public static final String TABLENAME = "KEY_VALUE_GD";

    /**
     * Properties of entity KeyValueGD.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property Key = new Property(0, String.class, "key", true, "KEY");
        public final static Property Value = new Property(1, String.class, "value", false, "VALUE");
    }


    public KeyValueGDDao(DaoConfig config) {
        super(config);
    }
    
    public KeyValueGDDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"KEY_VALUE_GD\" (" + //
                "\"KEY\" TEXT PRIMARY KEY NOT NULL UNIQUE ," + // 0: key
                "\"VALUE\" TEXT);"); // 1: value
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"KEY_VALUE_GD\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, KeyValueGD entity) {
        stmt.clearBindings();
        stmt.bindString(1, entity.key);
 
        String value = entity.value;
        if (value != null) {
            stmt.bindString(2, value);
        }
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, KeyValueGD entity) {
        stmt.clearBindings();
        stmt.bindString(1, entity.key);
 
        String value = entity.value;
        if (value != null) {
            stmt.bindString(2, value);
        }
    }

    @Override
    public String readKey(Cursor cursor, int offset) {
        return cursor.getString(offset + 0);
    }    

    @Override
    public KeyValueGD readEntity(Cursor cursor, int offset) {
        KeyValueGD entity = new KeyValueGD();
        readEntity(cursor, entity, offset);
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, KeyValueGD entity, int offset) {
        entity.key = cursor.getString(offset + 0);
        entity.value = cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1);
     }
    
    @Override
    protected final String updateKeyAfterInsert(KeyValueGD entity, long rowId) {
        return entity.key;
    }
    
    @Override
    public String getKey(KeyValueGD entity) {
        if(entity != null) {
            return entity.key;
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(KeyValueGD entity) {
        throw new UnsupportedOperationException("Unsupported for entities with a non-null key");
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
