package vn.com.vng.zalopay.data.cache.model;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "ZFL".
*/
public class ZFLDao extends AbstractDao<ZFL, Long> {

    public static final String TABLENAME = "ZFL";

    /**
     * Properties of entity ZFL.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property ZaloId = new Property(0, long.class, "zaloId", true, "_id");
        public final static Property UserName = new Property(1, String.class, "userName", false, "USER_NAME");
        public final static Property Avatar = new Property(2, String.class, "avatar", false, "AVATAR");
        public final static Property Birthday = new Property(3, String.class, "birthday", false, "BIRTHDAY");
        public final static Property DisplayName = new Property(4, String.class, "displayName", false, "DISPLAY_NAME");
        public final static Property NormalizeDisplayName = new Property(5, String.class, "normalizeDisplayName", false, "NORMALIZE_DISPLAY_NAME");
        public final static Property Gender = new Property(6, long.class, "gender", false, "GENDER");
        public final static Property UsingApp = new Property(7, boolean.class, "usingApp", false, "USING_APP");
    }


    public ZFLDao(DaoConfig config) {
        super(config);
    }
    
    public ZFLDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"ZFL\" (" + //
                "\"_id\" INTEGER PRIMARY KEY NOT NULL ," + // 0: zaloId
                "\"USER_NAME\" TEXT," + // 1: userName
                "\"AVATAR\" TEXT," + // 2: avatar
                "\"BIRTHDAY\" TEXT," + // 3: birthday
                "\"DISPLAY_NAME\" TEXT," + // 4: displayName
                "\"NORMALIZE_DISPLAY_NAME\" TEXT," + // 5: normalizeDisplayName
                "\"GENDER\" INTEGER NOT NULL ," + // 6: gender
                "\"USING_APP\" INTEGER NOT NULL );"); // 7: usingApp
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"ZFL\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, ZFL entity) {
        stmt.clearBindings();
        stmt.bindLong(1, entity.zaloId);
 
        String userName = entity.userName;
        if (userName != null) {
            stmt.bindString(2, userName);
        }
 
        String avatar = entity.avatar;
        if (avatar != null) {
            stmt.bindString(3, avatar);
        }
 
        String birthday = entity.birthday;
        if (birthday != null) {
            stmt.bindString(4, birthday);
        }
 
        String displayName = entity.displayName;
        if (displayName != null) {
            stmt.bindString(5, displayName);
        }
 
        String normalizeDisplayName = entity.normalizeDisplayName;
        if (normalizeDisplayName != null) {
            stmt.bindString(6, normalizeDisplayName);
        }
        stmt.bindLong(7, entity.gender);
        stmt.bindLong(8, entity.usingApp ? 1L: 0L);
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, ZFL entity) {
        stmt.clearBindings();
        stmt.bindLong(1, entity.zaloId);
 
        String userName = entity.userName;
        if (userName != null) {
            stmt.bindString(2, userName);
        }
 
        String avatar = entity.avatar;
        if (avatar != null) {
            stmt.bindString(3, avatar);
        }
 
        String birthday = entity.birthday;
        if (birthday != null) {
            stmt.bindString(4, birthday);
        }
 
        String displayName = entity.displayName;
        if (displayName != null) {
            stmt.bindString(5, displayName);
        }
 
        String normalizeDisplayName = entity.normalizeDisplayName;
        if (normalizeDisplayName != null) {
            stmt.bindString(6, normalizeDisplayName);
        }
        stmt.bindLong(7, entity.gender);
        stmt.bindLong(8, entity.usingApp ? 1L: 0L);
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.getLong(offset + 0);
    }    

    @Override
    public ZFL readEntity(Cursor cursor, int offset) {
        ZFL entity = new ZFL();
        readEntity(cursor, entity, offset);
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, ZFL entity, int offset) {
        entity.zaloId = cursor.getLong(offset + 0);
        entity.userName = cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1);
        entity.avatar = cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2);
        entity.birthday = cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3);
        entity.displayName = cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4);
        entity.normalizeDisplayName = cursor.isNull(offset + 5) ? null : cursor.getString(offset + 5);
        entity.gender = cursor.getLong(offset + 6);
        entity.usingApp = cursor.getShort(offset + 7) != 0;
     }
    
    @Override
    protected final Long updateKeyAfterInsert(ZFL entity, long rowId) {
        entity.zaloId = rowId;
        return rowId;
    }
    
    @Override
    public Long getKey(ZFL entity) {
        if(entity != null) {
            return entity.zaloId;
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(ZFL entity) {
        throw new UnsupportedOperationException("Unsupported for entities with a non-null key");
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
