<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#aa000000"
    android:gravity="center"
    android:orientation="vertical">

    <FrameLayout
        android:id="@+id/layoutLinkCard"
        android:layout_width="match_parent"
        android:layout_height="192dp"
        android:layout_gravity="center"
        android:layout_marginLeft="@dimen/spacing.medium.t"
        android:layout_marginRight="@dimen/spacing.medium.t"
        android:background="@drawable/bg_link_card_detail"
        android:baselineAligned="false">

        <ImageView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="top|right"
            android:adjustViewBounds="true"
            android:scaleType="centerInside"
            android:src="@drawable/ic_map"/>

        <ImageView
            android:id="@+id/iv_logo"
            android:layout_width="wrap_content"
            android:layout_height="30dp"
            android:layout_marginLeft="18dp"
            android:layout_marginTop="18dp"
            android:src="@drawable/ic_visa"/>

        <com.zalopay.ui.widget.IconFont
            android:id="@+id/tv_num_acc"
            style="@style/TextView.Default"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginLeft="18dp"
            android:layout_marginTop="142dp"
            android:shadowColor="#80000000"
            android:shadowDx="0"
            android:shadowDy="4"
            android:shadowRadius="4"
            android:textColor="@color/white"
            android:textSize="@dimen/textsize.card.number"
            android:textStyle="normal"
            app:typefaceAsset="ocr-a-tribute"
            tools:text="*** 0014"/>
    </FrameLayout>

    <Button
        android:id="@+id/layoutRemoveLink"
        style="@style/Button.Default.New"
        android:layout_width="match_parent"
        android:layout_height="@dimen/button.height"
        android:text="@string/txt_remove_link"/>

</LinearLayout>